
name: Notify Team on Push

on:
  push:
    branches:
      - "*" # Barcha branch'larga push bo'lganda ishlaydi

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram Notification
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.8111927597:AAHYtseJ0ktGxJF9lW4I43oYQlqnuNkXGrI }}
          TELEGRAM_CHAT_ID: ${{ secrets.1002504976072 }}
        run: |
          # Xabar matnini tayyorlash
          MESSAGE="🔔 Yangi push! \n👤 Foydalanuvchi: ${{ github.actor }}\n📂 Repository: ${{ github.repository }}\n🌿 Branch: ${{ github.ref_name }}\n🔗 Commit: ${{ github.event.head_commit.url }}\n\nPull oling!"
          
          # Telegram API orqali xabar yuborish
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="${MESSAGE}" \
            -d parse_mode="Markdown"
